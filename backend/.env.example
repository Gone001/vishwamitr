# Copy to backend/.env and fill in (do not commit .env).
# URL: Project Settings → API → Project URL (https://YOUR_REF.supabase.co — not the dashboard link)
# Key: Project Settings → API → anon public (or service_role for server-only, keep secret)
SUPABASE_URL=https://mxgipylcfcrarxqvsvqt.supabase.co
SUPABASE_KEY=your-anon-key-here
